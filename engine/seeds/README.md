# Engine destination seeds

JSON bundles for **`KeywordDiscoveryJob`** cold start (`seed_mode: destination_default`).

**Specification:** [docs/02-engine/seed-data-spec.md](../../docs/02-engine/seed-data-spec.md)

- `{destination}.json` — `destination` matches `tenants.destination` (e.g. `bali`, `thailand`, `europe`, `caribbean`, `other`).
